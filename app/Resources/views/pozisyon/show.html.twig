{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pozisyon</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pozisyon.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pozisyon_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('pozisyon_edit', { 'id': pozisyon.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
